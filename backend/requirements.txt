# Runtime dependencies for the backend API (production image).
# Dev/test tools (pytest, ruff, aiosqlite) live in requirements-dev.txt so they
# are never installed into the production image. Keep this list minimal: every
# package here ships to production and widens the attack surface.

# Web framework
fastapi==0.104.1
uvicorn==0.24.0
pydantic==2.5.0

# Database — async Postgres driver (asyncpg). The sync psycopg2 driver is not
# used; tests use aiosqlite (a dev dependency).
sqlalchemy==2.0.23
asyncpg==0.30.0  # 0.30 is the first release with prebuilt Python 3.13 wheels

# Data processing (pulls numpy transitively) & HTTP client (SDK + alerting)
pandas>=2.2.0
httpx==0.25.2

# Per-client rate limiting
limits>=3.6,<6
